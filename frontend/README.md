# Frontend Folder Guide

This folder contains storefront static assets used by public pages.

## Subfolders

- `assets/`: Shared styles, scripts, images, and videos.

## Notes

- Public PHP pages in project root consume these assets.
- Keep asset paths relative and deployment-safe (no hardcoded host assumptions where possible).
