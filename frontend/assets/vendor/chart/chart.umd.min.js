(function (global) {
  if (!global || global.Chart) {
    return;
  }

  function ChartFallback() {
    this.destroy = function () {};
  }

  global.Chart = ChartFallback;
})(typeof window !== "undefined" ? window : this);
